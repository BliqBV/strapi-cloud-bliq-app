'use strict';

/**
 * subscription-powerflow controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::subscription-powerflow.subscription-powerflow');
