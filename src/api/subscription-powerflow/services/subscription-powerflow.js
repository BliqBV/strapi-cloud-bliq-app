'use strict';

/**
 * subscription-powerflow service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::subscription-powerflow.subscription-powerflow');
