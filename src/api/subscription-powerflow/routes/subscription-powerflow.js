'use strict';

/**
 * subscription-powerflow router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::subscription-powerflow.subscription-powerflow');
