'use strict';

/**
 * powerflow-subscription controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::powerflow-subscription.powerflow-subscription');
